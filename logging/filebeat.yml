filebeat.inputs:
  - type: filestream
    enabled: true
    paths:
      - /logs/application.log
    parsers:
      - ndjson:
          target: ""
          overwrite_keys: true

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]

setup.kibana:
  host: "http://kibana:5601"